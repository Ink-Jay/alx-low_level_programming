#include <stdio.h>

/*
 * main - prints alphabet in lower case
 * Return: 0 when succesfully executed
 */

int main(void)
{
char alph;

for(alph = 'a'; alph <= 'z'; alph++)
{
putchar(alph)
}
putchar('\n');
return (0);
