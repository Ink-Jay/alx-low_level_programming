#include "main.h"
#include <stdio.h>

/**
  * _isdigit - checks for a digit
  * @c: character to be checked
  * Return: 1 if digit and 0 otherewise
  */
int _isdigit(int c)
{
return (c >= 57 && c <= 57)
}
